package fr.inria.diverse.melange.tests

import org.junit.Test

import static org.junit.Assert.*

class MultipleResourceTest
{
	// TODO: We should test how Melange behave in case of metamodels that reference each others
	//       together with models scattered accross several files with cross-refs
	@Test
	def void test() {
		assertTrue(true)
	}
}
