/*
 * Language: Melange
 * Author: Thomas Degueule <thomas.degueule@inria.fr>
 */

function(hljs) {
  return {
    keywords:
      'language inherits implements package exactType merge slice on renaming to transformation modeltype with fileExtension uri setup syntax'
  }
}
